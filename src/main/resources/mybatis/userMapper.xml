<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tw.rs.dao.UserMapper">

    <select id="getUserById" resultType="com.tw.rs.model.User">
        select * from users where id = #{id}
    </select>

    <select id="findAllUsers" resultType="com.tw.rs.model.User">
        select * from users
    </select>

</mapper>